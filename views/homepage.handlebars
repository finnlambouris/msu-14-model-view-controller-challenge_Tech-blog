{{#each blog as |post|}}
<div class="blogpost">
    <a class="blog-links" href="/blogpost/{{id}}">
        <h2>{{title}}</h2>
        <h3>Posted by {{user.username}} on {{format_date createdAt}}</h3>
        <p>{{post_preview body}}</p>
    </a>
</div>
{{/each}}